<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title>Super mock</title>
  <link href="/v1/css/mock.css" rel="stylesheet">
  <link href="/v1/css/index.css" rel="stylesheet">
</head>
<body class="bg-cover bg-center bg-fixed text-white flex flex-col gap-5">
  <!-- background overlay -->
  <div class="w-full h-full fixed bg-black top-0 -z-10 bg-opacity-50"></div>

  <div class="flex flex-col gap-5 py-5">
    <h1 class="text-center text-3xl font-bold">Calm & ready</h1>
    <p class="text-xl text-center font-medium">Keep calm and get ready for what's coming</p>
    <p class="text-center">You're about to listen to the best music of all time</p>
  </div>

  <div class="w-full flex flex-wrap justify-center gap-10 pb-10 pt-2">
    <!-- login form -->
    <div class="w-full md:w-1/3 bg-gray-900 bg-opacity-90 mx-5 md:mx-0 p-5">
      <form action="/v1/users/login" method="POST" class="flex flex-col gap-4">
        <h2 class="text-center text-2xl font-medium">Login</h2>
        <p class="text-xl">Already a user?</p>
        <div class="w-full">
          <label class="w-full" for="register-username">Username</label>
          <input type="text" name="username" id="register-username" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-password">Password</label>
          <input type="password" name="password" id="register-password" class="w-full">
        </div>
        <% if (locals.loginErrors && loginErrors.length > 0) { %>
          <p class="text-red-500">
            <% for (const error of loginErrors) { %>
              <span><%= error %></span><br/>
            <% } %>
          </p>
        <% } %>
        <div class="flex justify-center">
          <button type="submit" class="px-4 py-2 bg-black">Login</button>
        </div>
      </form>
    </div>

    <!-- registration form -->
    <div class="w-full md:w-1/3 bg-gray-900 bg-opacity-90 mx-5 md:mx-0 p-5">
      <form action="/v1/users/register" method="POST" class="flex flex-col gap-4">
        <h2 class="text-center text-2xl font-medium">Register</h2>
        <% if (locals.successfulRegistration) { %>
        <p class="text-center text-green-300">
          <span>Successfully registered!</span><br/>
          <span>You may now log in</span>
        </p>
        <% } else { %>
        <p class="text-xl">Don't have an account?</p>
        <% } %>
        <div class="w-full">
          <label class="w-full" for="register-first-name">First name</label>
          <input type="text" name="firstName" id="register-first-name" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-last-name">Last name</label>
          <input type="text" name="lastName" id="register-last-name" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-dob">Date of birth</label>
          <input type="date" name="dob" id="register-dob">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-email">Email</label>
          <input type="email" name="email" id="register-email" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-username">Username</label>
          <input type="text" name="username" id="register-username" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-password">Password</label>
          <input type="password" name="password" id="register-password" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-password-confirmation">Password confirmation</label>
          <input type="password" name="passwordConfirmation" id="register-password-confirmation" class="w-full">
        </div>
        <div class="w-full">
          <label class="w-full" for="register-gender">Gender</label>
          <select name="gender" id="register-gender">
            <option value="RATHER_NOT_SAY" selected>Rather not say</option>
            <option value="MALE">Male</option>
            <option value="FEMALE">Female</option>
          </select>
        </div>
        <% if (locals.registrationErrors && registrationErrors.length > 0) { %>
          <p class="text-red-500">
            <% for (const error of registrationErrors) { %>
              <span><%= error %></span><br/>
            <% } %>
          </p>
        <% } %>
        <div class="flex justify-center">
          <button type="submit" class="px-4 py-2 bg-black">Register</button>
        </div>
      </form>
    </div>
  </div>
  <footer class="fixed bottom-0 right-0 p-4 bg-black bg-opacity-50 text-xs flex flex-col gap-1 pointer-events-none" dir="rtl">
    <span>This is a mock application</span>
    <span>It's open source and its sole purpose is to learn and practice</span>
    <span class="font-medium">It's not production-ready and intentionally lacks of high standards</span>
    <span>More information <a href="https://github.com/BenjaminGuzman/webmock" target="_blank" class="hover:underline text-blue-400">here</a></span>
</footer>
</body>
</html>
